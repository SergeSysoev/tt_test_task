const arrayToString = (numbersArray) => {
    if (numbersArray.length === 1) {
        return numbersArray[0].toString();
    }

    let result = numbersArray[0];

    //

    return result;
};

exports.arrayToString = arrayToString;
